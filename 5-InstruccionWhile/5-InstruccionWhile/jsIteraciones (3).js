function Mostrar()
{
    var clave;

    clave = prompt("ingrese la clave:","");

    while(clave != "utn750" && clave != null) 
    {
        
            clave = prompt("Ingrese la clave");    
        
    }
    
    /* cuando presionamos cancelar a traves del prompt enviamos 
       un valor tipo null que se guarda en la variable clave por estar razon
       lo validamos de forma que entre que cundo sea difernte de null o diferente
       de "utn750" entre al bucle y me pida la clave correcta.*/

    }//FIN DE LA FUNCIÓN
